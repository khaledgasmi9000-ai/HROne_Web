<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>HR One - Dashboard RH</title>
  <link rel="stylesheet" href="{{ asset('styles.css') }}">
</head>
<body>
  <div class="page">
    <div class="shell">
      <header class="topbar">
        <a class="brand" href="{{ path('homepage') }}">
          <div class="brand-logo">
            <img src="{{ asset('logo.png') }}" alt="HR One">
          </div>
          <div>
            <p class="brand-name">HR One</p>
            <p class="brand-sub">Dashboard RH</p>
          </div>
        </a>
        <div class="nav-actions">
          <a class="nav-link" href="{{ path('logout') }}">Se deconnecter</a>
        </div>
      </header>

      <main class="layout">
        <section class="hero">
          <div class="hero-inner">
            <span class="eyebrow">Espace RH</span>
            <h1>Bienvenue {{ user ? user.NomUtilisateur : 'RH' }}.</h1>
            <p class="hero-copy">
              Votre authentification fonctionne. Cette page confirme que le compte RH
              est bien connecte et redirige correctement apres login.
            </p>
            <div class="metrics">
              <div class="metric">
                <strong>{{ user ? user.Email : '-' }}</strong>
                <span>Email connecte.</span>
              </div>
              <div class="metric">
                <strong>{{ user and user.entreprise ? user.entreprise.NomEntreprise : '-' }}</strong>
                <span>Entreprise associee.</span>
              </div>
              <div class="metric">
                <strong>RH</strong>
                <span>Profil detecte en session.</span>
              </div>
            </div>
          </div>
        </section>

        <section class="form-side">
          <div class="form-shell">
            <div class="panel-head">
              <h2>Connexion RH reussie</h2>
              <p>La prochaine etape sera de brancher ici vos modules RH reels.</p>
            </div>
            <div class="trust-row">
              <span class="trust-chip">Auth OK</span>
              <span class="trust-chip">Session active</span>
              <span class="trust-chip">Twig OK</span>
            </div>
          </div>
        </section>
      </main>
    </div>
  </div>
</body>
</html>
