liip_imagine:
  driver: "gd"
  resolvers:
    default:
      web_path: ~
  filter_sets:
    cache: ~
    thumb_300:
      quality: 80
      filters:
        thumbnail: { size: [300, 300], mode: inset }
